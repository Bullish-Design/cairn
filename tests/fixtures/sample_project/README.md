# Sample Project (live fixture)

This tree is a real on-disk fixture used to seed fsdantic workspaces in the
Cairn test suite.  It exercises nested directories, binary and text payloads,
and a legacy file that agents are expected to delete via overlay tombstones.
