
This walkthrough was generated by ``python -m demo`` — every block
below is a real capture from the run that produced it.  Nothing here
touched a real checkout: the fixture, cairn homes, and workspaces
live only under ``demo/out/``.

**Run environment** — bubblewrap ``/nix/store/g7svy17fhkg2cq3q4lfzzc0mmsl3d8hq-bubblewrap-0.11.2/bin/bwrap``; sandbox interpreter
``/nix/store/151hdz9jp2yzn4svgb4ahc12rqp53fxf-python3-3.13.13/bin/python3``; sandbox mode: **fallback runtime mounts (no closure manifest)**.

## Act 0 — Trust the runtime

### Chapter 00: cairn doctor

Before any agent runs, the walkthrough verifies the sandbox runtime.
``cairn doctor`` answers the question that a green test suite cannot:
*do the parts that must agree actually agree?*  It greps the argv the
executor builds for the isolation flags, and then — the part that
matters — launches a real sandbox and asserts from inside it that the
host home is unreachable and the network is unreachable.

```text
$ $REPO/.devenv/state/venv/bin/python -m cairn.cli.cli doctor --cairn-home $REPO/demo/out/home --project-root $REPO/demo/out/project
[ok  ] bwrap binary: bubblewrap 0.11.2 (/nix/store/g7svy17fhkg2cq3q4lfzzc0mmsl3d8hq-bubblewrap-0.11.2/bin/bwrap)
[ok  ] sandbox python: /nix/store/151hdz9jp2yzn4svgb4ahc12rqp53fxf-python3-3.13.13/bin/python3.13 (configured)
[skip] closure manifest: not configured; sandbox binds the fallback runtime mounts
[ok  ] isolation flags: --unshare-all, --die-with-parent, --new-session, --clearenv
[ok  ] materialization: reflink (same filesystem)
[ok  ] sandbox launch: sandbox starts and executes code
[ok  ] isolation effective: verified from inside the sandbox: no host home, no network
```

Two checks launch a real sandbox rather than reading config:
``sandbox launch`` (a probe process runs and prints) and
``isolation effective`` (a probe *inside* the sandbox reports whether
the host home or the network leaked through).  Everything else here
is configuration inspection — these two are the evidence.

[ok] doctor exits 0 on a healthy runtime

[ok] isolation verified from inside a real sandbox

## Act I — The core loop

### Chapter 01: The fixture

Every chapter drives agents against the same throwaway project under
``demo/out/project/``.  Nothing in this walkthrough ever touches your
checkout — the runner aborts if ``CAIRN_PATHS_*`` is set, and every
orchestrator asserts it is bound to the fixture before spawning
anything.

The fixture is a git repository whose entries each exist to make one
line of the walkthrough true.  ``cairn`` snapshots the tree with
``capture_manifest`` — gitignore-aware, no symlink following — and
that snapshot is what an agent's disposable workspace is materialized
from:

```text
$ git -C $REPO/demo/out/project ls-files
.gitignore
README.md
link_to_main
run.sh
src/doomed.py
src/main.py
src/util.py
```

```text
manifest entries
.gitignore         file  0o644  18b
README.md          file  0o644  109b
empty_dir          dir/
link_to_main       symlink -> src/main.py
run.sh             file  0o755  58b
src                dir/
src/doomed.py      file  0o644  107b
src/main.py        file  0o644  148b
src/util.py        file  0o644  98b
secrets.env        (absent — gitignored, never materialized)
```

```text
tree digest (baseline)
8663662f3f69c428
```

[ok] gitignore filtering: secrets.env is not in the manifest

[ok] symlink preserved as a symlink (not dereferenced)

[ok] empty_dir/ survives materialization (present as a dir)

[ok] run.sh keeps its 0o755 mode in the manifest

### Chapter 02: Providers

Code reaches an agent through a ``CodeProvider``: ``get_code`` returns
the task source, ``validate_code`` approves it.  Two ship built in —
``FileCodeProvider`` (the reference is a path to a ``.py`` file) and
``InlineCodeProvider`` (the reference *is* the code) — and plugins
register under the ``cairn.providers`` entry-point group.

A provider receives a small context.  The ``workspace`` entry is a
read-only ``ProjectView`` over the canonical tree — never a writable
database.  Let's look at the context a real agent run produces:

```python
class RecordingProvider:
    def __init__(self): self.last_context = None
    async def get_code(self, reference, context):
        self.last_context = context
        return reference
    async def validate_code(self, code):
        return True, None
```

```text
provider context
agent_id     = 'agent-bcc418d0'  (isinstance str: True)
project_root = $REPO/demo/out/project  (== fixture: True)
workspace    = ProjectView  (hasattr write: False)
```

Two failure modes matter.  A provider error must fail the *agent*,
not the orchestrator — the worker keeps running.  And an unknown
plugin name must produce the entry-point error, not a silent fallback:

```text
resolve_code_provider('nonexistent-plugin')
ProviderError: [PROVIDER_ERROR] Unknown provider 'nonexistent-plugin'. Install the plugin package to use it.
```

```text
agent with a missing reference
agent_id = agent-82b62c96
state    = errored
error    = [PROVIDER_ERROR] Code reference not found: $REPO/demo/out/project/no-op.py
```

[ok] a provider error lands the agent in ERRORED, not the orchestrator

[ok] the error code is snake-cased [PROVIDER_ERROR]

[ok] the worker survives a provider error

### Chapter 03: Run an agent

An agent run is a state machine: ``QUEUED → GENERATING → EXECUTING →
SUBMITTING → REVIEWING``.  The provider we wrote above sleeps in
``get_code`` and the task sleeps too, so every phase lasts long
enough to observe.  The task is ordinary Python that runs inside
bubblewrap against a disposable copy of the tree:

```python
import time
time.sleep(2.0)                                   # make the lifecycle visible
write_file("src/main.py", 'print("edited by the agent")\n')
submit_result("updated the greeting", ["src/main.py"])
```

```text
lifecycle states for agent-04a11556
queued      + 0.001s
generating  + 0.007s
executing   + 0.421s
submitting  + 2.512s
reviewing   + 2.524s
MISSED = none
```

[ok] all five lifecycle states were observed

[ok] the agent settles in REVIEWING

[ok] the computed changeset recorded src/main.py (1 write)

### Chapter 04: The tree is untouched

Here is the headline.  Agents run against a *disposable copy* of the
tree — copy-on-write where the filesystem supports it.  Nothing the
agent does reaches the working tree until a human accepts.  We
measure the tree with a content digest over the manifest:

```text
tree digest BEFORE
8663662f3f69c428
```

```text
tree digest AFTER RUN
8663662f3f69c428  (unchanged: True)
```

[ok] a run does not touch the tree

```text
accept stats
{"files_deleted": 0, "files_written": 1}
```

```text
tree digest AFTER ACCEPT
e172b6470e8bea1a  (changed: True)
```

[ok] accept changes the tree

[ok] the accepted file landed in the tree

### Chapter 05: The review surface

Before anything is integrated, the human sees the review surface: the
agent's disposable workspace, diffed against the current tree.  The
diff is the *authoritative* changeset — computed by the executor from
what the agent actually did, not from what the agent says it did
(chapter 09 pushes on exactly that seam).  The public way to read it
is ``cairn preview changes``:

The workspace itself contains the sandbox scaffolding (``.cairn/``)
alongside the materialized tree; the diff filters it out — the review
surface shows only what would change on accept:

```text
workspace top-level
.cairn, .gitignore, README.md, empty_dir, link_to_main, run.sh, src
```

```text
$ $REPO/.devenv/state/venv/bin/python -m cairn.cli.cli preview changes agent-61ea0a5f --cairn-home $REPO/demo/out/home --project-root $REPO/demo/out/project
added      src/new_module.py
modified   src/main.py
removed    src/doomed.py

total changes: 3
```

[ok] preview lists the added module

[ok] preview lists the modified file

[ok] preview lists the deleted file

[ok] no .cairn scaffolding leaks into the diff

### Chapter 06: Accept

Accept applies the computed changeset to the real working tree —
under a per-project integration lock, with a durable journal and a
pre-apply snapshot so the whole thing is reversible (chapter 07).

```text
accept stats
{"files_deleted": 1, "files_written": 2}
```

```text
tree digest
before: e172b6470e8bea1a
after : f93fcf9d669b9ae4
```

[ok] accept applied 2 writes and 1 deletion

[ok] the tree changed

[ok] the deleted file is gone from the tree

[ok] accept discards the disposable workspace

### Chapter 07: Undo

Every accept is reversible.  Before applying, cairn snapshots the
pre-accept content of every touched path into ``bin.db`` under
``undo/{agent_id}/``; ``cairn undo`` restores the tree to that state.
The interesting case is an accept that *created* a file — undoing it
means deleting it, and the drift check must read the accepted state
(presence) correctly:

```text
accept agent-450f5ea0
stats        = {"files_deleted": 0, "files_written": 1}
brand_new.py exists after accept: True
```

```text
undo agent-450f5ea0
stats                  = {"deleted": 1, "restored": 0}
brand_new.py exists    : False
src/main.py untouched  : True
```

[ok] undo restored the tree (added file removed)

Undo is fail-closed: it validates that the accepted state is still
present, and refuses (``UNDO_STALE_BASE``) rather than silently
overwriting a later human edit:

```text
undo over a human edit
UNDO_STALE_BASE  stale_paths=['src/human_edit_target.py']
```

[ok] undo refuses on drift (UNDO_STALE_BASE)

[ok] the human edit survives

### Chapter 08: Reject

Reject is the other side of review: the disposable workspace is
discarded and the tree is untouched.  Nothing was integrated, so
there is nothing to undo.

```text
agent agent-2bfc5092 in review
state = reviewing
workdir exists: True
```

```text
after reject agent-2bfc5092
workdir exists     : False
tree digest changed: False
never_land.py      : False
```

[ok] reject discards the workspace

[ok] reject leaves the tree untouched

## Act II — Where Cairn earns its keep

### Chapter 09: The agent lies

Agents are fallible, and sometimes they lie.  The submission payload
(``summary`` + ``changed_files``) is the agent's *self-report*; the
changeset the executor computed from the workspace diff is the truth.
Here is an agent that wrote a file and claimed it did something else
entirely:

```python
write_file("src/sneaky.py", 'print("I should not be here")\n')
submit_result("fixed a typo in the README", ["README.md"])
```

```text
agent agent-c3807399
written        = ['src/sneaky.py']
claim_mismatch = True
submission     = {"changed_files": ["README.md"], "submitted_at": 1786294334.1297781, "summary": "fixed a typo in the README"}
```

[ok] the computed changeset recorded src/sneaky.py

[ok] the self-report is flagged as a mismatch

### Chapter 10: Fail-closed accept

Accept revalidates the base every touched path had at run start.  If
a human changed the tree while the agent was working, accept refuses
with ``ACCEPT_STALE_BASE`` — it never silently overwrites a newer
edit.  The agent stays usable, and ``--force`` explicitly lets the
agent's version win:

```text
accept without --force
ACCEPT_STALE_BASE  stale_paths=['src/main.py']
```

[ok] accept refused with ACCEPT_STALE_BASE

[ok] the human edit is untouched

```text
agent after the refusal
state = reviewing
```

[ok] the agent stays usable (still REVIEWING)

```text
accept with --force
{"files_deleted": 0, "files_written": 2}
```

[ok] --force accepts and the agent's version wins

### Chapter 11: The boundary, probed

What can task code actually reach?  The answer is measured from
inside the sandbox, not assumed.  ``bwrap`` runs the task as an
unprivileged user with only the materialized workspace writable.
Note two framings that matter.  First: ``--clearenv`` is passed, but
bubblewrap repopulates a handful of variables — the environment is
*small*, not empty.  Second: the raw ``open("../escape.txt")`` below
*succeeds* — the sandbox helpers are ergonomics, not the boundary.
The write lands in an ephemeral mount outside the workspace, so it
appears in no changeset and evaporates with the sandbox.  Confinement
comes from what is mounted, not from what is validated:

```python
import json
import os
import socket
import sys

probe = {}
probe["environ_entries"] = len(os.environ)
probe["environ"] = dict(os.environ)
probe["stdin_isatty"] = sys.stdin.isatty()
try:
    os.listdir(os.path.expanduser("~/.ssh"))
    probe["home_ssh_readable"] = True
except OSError:
    probe["home_ssh_readable"] = False
try:
    socket.create_connection(("1.1.1.1", 53), timeout=2)
    probe["network"] = "connected"
except OSError:
    probe["network"] = "blocked (OSError)"
try:
    with open("/etc/passwd", "w") as fh:
        fh.write("pwned")
    probe["write_etc_passwd"] = "WROTE"
except OSError as exc:
    probe["write_etc_passwd"] = f"blocked ({type(exc).__name__})"
try:
    with open("../escape.txt", "w") as fh:
        fh.write("raw open escapes the helpers")
    probe["raw_escape_write"] = "WROTE"
except OSError as exc:
    probe["raw_escape_write"] = f"blocked ({type(exc).__name__})"
try:
    write_file("../escape.txt", "helper is the boundary")
    probe["helper_escape_write"] = "WROTE"
except Exception as exc:
    probe["helper_escape_write"] = f"{type(exc).__name__}: {exc}"
probe["uid"] = os.getuid()
write_file("probe.json", json.dumps(probe, sort_keys=True))
print(json.dumps(probe, indent=2, sort_keys=True))
submit_result("probed the boundary", [])
```

```text
sandbox probe output (agent agent-69d1ee6f)
{
  "environ": {
    "CAIRN_MAX_CPU_SECONDS": "60.0",
    "CAIRN_MAX_MEMORY_BYTES": "104857600",
    "CAIRN_MAX_OPEN_FILES": "1024",
    "CAIRN_MAX_OUTPUT_FILE_BYTES": "67108864",
    "CAIRN_MAX_PROCESSES": "64",
    "CAIRN_MAX_RECURSION_DEPTH": "1000",
    "CAIRN_WORKSPACE": "/workspace",
    "LC_CTYPE": "C.UTF-8",
    "PATH": "/usr/bin:/bin",
    "PWD": "/",
    "PYTHONUNBUFFERED": "1"
  },
  "environ_entries": 11,
  "helper_escape_write": "ValueError: Path traversal not allowed in sandbox: '../escape.txt'",
  "home_ssh_readable": false,
  "network": "blocked (OSError)",
  "raw_escape_write": "WROTE",
  "stdin_isatty": false,
  "uid": 65534,
  "write_etc_passwd": "blocked (FileNotFoundError)"
}
```

Reading the probe's answers: the environment holds a handful of
entries bubblewrap needs (not zero, and certainly not the host's
env), stdin is not a tty, the host's ``~/.ssh`` is not reachable,
the network is blocked, and ``/etc/passwd`` is not writable.

[ok] the environment is tiny (no host env leaked)

[ok] no host environment leaked (only cairn-injected + sandbox internals)

[ok] stdin is not a tty

[ok] the host home is unreachable

[ok] the network is blocked

[ok] /etc/passwd is not writable

[ok] the sandbox runs as uid 65534 (nobody)

[ok] the write_file helper rejects traversal

The contrast is the point: the *helper* refused ``../escape.txt``,
while raw ``open`` wrote it — and that write is unreachable.  It is
not in the agent's changeset, and nothing on the host has it:

```text
$ find $REPO/demo/out -name escape.txt
```

[ok] the raw escape write left nothing on the host

### Chapter 12: Limits

Limits are enforced by the host and legible afterwards.  A task that
loops forever is killed at ``max_execution_time``; a task that asks
for 400 MB against a 100 MB ``max_memory_bytes`` cap dies with
``MemoryError`` — and the partial log survives via the mirror, so
``cairn logs`` works on errored agents:

```text
timeout agent agent-c0ceeff0
state = errored
error = [EXECUTION_TIMEOUT] Operation exceeded timeout of 3.0s
```

```text
run.log tail (agent-c0ceeff0)
looping forever
looping forever
looping forever
looping forever
looping forever
looping forever

[cairn] killed after 3.0s
```

[ok] the infinite loop was killed with EXECUTION_TIMEOUT

[ok] the kill reason is in the log

```text
memory agent agent-d723d89f
state = errored
error = [SANDBOX_EXECUTION_FAILED] Sandbox execution failed (exit 1): MemoryError
```

```text
run.log (agent-d723d89f)
Traceback (most recent call last):
  File "/workspace/.cairn/boot.py", line 297, in main
    return _run_task()
  File "/workspace/.cairn/boot.py", line 286, in _run_task
    exec(code, namespace)  # noqa: S102 — task code execution is the sandbox's purpose
    ~~~~^^^^^^^^^^^^^^^^^
  File "/workspace/.cairn/task.py", line 1, in <module>
    blob = bytearray(400 * 1024 * 1024)
MemoryError
```

[ok] the 400MB request failed against the 100MB cap

```text
$ $REPO/.devenv/state/venv/bin/python -m cairn.cli.cli logs agent-c0ceeff0 --cairn-home $REPO/demo/out/home --project-root $REPO/demo/out/project
looping forever
looping forever
looping forever
looping forever
looping forever
looping forever

[cairn] killed after 3.0s
```

[ok] cairn logs shows the errored agent's partial log

### Chapter 13: A task cannot steer its changeset

Admission rules are host state.  A task that rewrites ``.gitignore``
to ``*.py`` cannot retroactively hide the ``.py`` file it just wrote
— the changeset is computed against a filter built from the *project*
tree, never from what the task writes.  The diff does not care about
the task's self-reported intent:

```python
write_file(".gitignore", "*.py")
write_file("src/still_counted.py", "print('I still count')\n")
submit_result("cleaned up", ["src/still_counted.py", ".gitignore"])
```

```text
agent agent-5d4e276d
workspace .gitignore : '*.py'
workspace src/       : ['human_edit_target.py', 'main.py', 'new_module.py', 'still_counted.py', 'util.py']
executor changeset   : written=['.gitignore', 'src/still_counted.py']
```

The naive diff a tool might compute from the *workspace's* own
``.gitignore`` would lie here — with ``*.py`` in effect, the `.py`
files would vanish from its view of the workspace.  That is exactly
why the executor's admission filter is host state, bound once per run
to the project tree and never rebuilt from anything the task wrote.

[ok] the task-written .gitignore is part of the changeset

[ok] the new rule does not hide the .py file

### Chapter 14: Concurrency

Two things about concurrency are worth measuring, and one is worth
narrating honestly.  First, the *dequeue* order of the priority
``TaskQueue`` is deterministic — URGENT before HIGH before NORMAL
before LOW, with FIFO tie-breaking:

```text
TaskQueue dequeue order
['urgent', 'high', 'normal', 'low']
```

[ok] dequeue order follows priority

Second, concurrency is *limited*: with ``max_concurrent_agents=2``,
five one-second tasks take roughly ``ceil(5/2) = 3`` seconds, not
one.  The claim is asserted by wall clock, not by completion order —
completion order is *not* deterministic, because a queue is not a
preemptive scheduler: tasks already running are not interrupted, so
even an URGENT task submitted late waits for a free slot.  That
subtlety is the honest version of the story:

```text
five 1s tasks at max_concurrent_agents=2
elapsed = 3.77s   (ceil(5/2) * 1s = 3.0s expected)
```

[ok] the wall clock reflects the concurrency cap

## Act III — Embedding APIs

Cairn is also a library.  These chapters embed it directly — no
daemon, no sandbox, all sub-second — and double as the maintained
replacement for the retired ``scripts/demo_cairn_library.py``.

### Chapter 15: open_workspace + WorkspaceInspector + WorkspaceStats

``open_workspace`` opens an fsdantic workspace with Cairn's
concurrency defaults (WAL, busy timeout); ``WorkspaceInspector``
reads one without touching it.  We create a small workspace, write a
couple of files, and inspect it read-only:

```python
ws = await open_workspace("demo/out/act3-workspaces/inspect.db")
await ws.files.write("src/alpha.py", "x = 1")
await ws.files.write("README.md", "# inspect me")

async with await WorkspaceInspector.from_path("demo/out/act3-workspaces/inspect.db") as inspector:
    stats = await inspector.stats()
    tree = await inspector.tree("/")
await ws.close()
```

```text
WorkspaceInspector
stats   = WorkspaceStats(file_count=2, dir_count=2, total_bytes=17)
listing = ['README.md', 'src']
read    = '# inspect me'
```

[ok] the inspector counts the files

[ok] the inspector lists the root

### Chapter 16: AgentStateManager — typed state, namespacing, atomic increment

``AgentStateManager`` gives one agent a namespaced key-value store
with typed round-trips and atomic counters.  Namespacing means two
agents can use the same key without colliding; the atomic
``increment`` means 50 concurrent increments land exactly 50 — no
lost updates:

```python
state = AgentStateManager(ws, "agent-1")
await state.set("last_file", "/src/main.py")
await state.set_typed("turn_state", TurnState(turn=1, context={}))
await state.increment("counter")
```

```text
AgentStateManager
agent_a last_file = '/src/main.py'
agent_b last_file = '/src/other.py'   (namespacing holds)
increment_turn    = 1
typed round-trip  = {'turn': 1, 'context': {'phase': 'review'}}
50 concurrent increments -> 50
```

[ok] namespacing keeps the agents' keys apart

[ok] typed models round-trip

[ok] 50 concurrent increments land exactly 50

### Chapter 17: WorkspaceCapability + ScriptedDriver + ProjectView

``WorkspaceCapability`` is the narrow, path-validated capability a
driver's model client receives — read/list/search/write/delete inside
one bounded root, never host paths.  ``ScriptedDriver`` executes an
explicit step plan under a hard ``step_limit``:

```python
cap = WorkspaceCapability(root)
driver = ScriptedDriver([
    DriverStep("write", "src/app.py", "print('hi')\n"),
    DriverStep("read", "src/app.py"),
    DriverStep("submit", "wrote the app", "src/app.py"),
])
submission = await driver.run("build the app", cap, step_limit=3)
```

```text
ScriptedDriver
submission (step_limit=3) = {'summary': 'wrote the app', 'changed_files': ['src/app.py']}
truncated (step_limit=2)  = {'summary': 'build the app', 'changed_files': ['src/app.py']}
src/app.py content        = "print('hi')\n"
```

[ok] the driver's writes land in the bounded root

[ok] the step limit is honored (submit never ran)

[ok] the capability refuses traversal

``ProjectView`` is the read-only snapshot providers receive (chapter
02): gitignore-aware and no-follow, with **no write surface at all** —
a provider physically cannot mutate the canonical tree:

```text
ProjectView over the fixture
hasattr(view, 'write') = False
list_dir('.')          = ['.gitignore', 'README.md', 'empty_dir', 'link_to_main', 'run.sh', 'src']
stat('run.sh')         = {'path': 'run.sh', 'kind': 'file', 'size': 58}
```

[ok] ProjectView exposes no write surface

[ok] ProjectView is gitignore-aware (no secrets.env)

[ok] ProjectView reports file metadata

### Chapter 18: TaskQueue + with_retry

``TaskQueue`` is the bounded priority queue the orchestrator runs on
(chapter 14 dequeue order, shown here via the public API), and
``with_retry`` / ``RetryStrategy`` absorb transient failures with
exponential backoff:

```python
queue = TaskQueue(max_size=10)
await queue.enqueue("b", TaskPriority.NORMAL)
await queue.enqueue("a", TaskPriority.URGENT)
first = await queue.dequeue_wait()   # -> a (URGENT beats NORMAL)

retry = RetryStrategy(max_attempts=3, initial_delay=0.01)
result = await retry.with_retry(flaky_operation, retry_exceptions=(RuntimeError,))
```

```text
TaskQueue + RetryStrategy
dequeue order = [urgent task, normal task, low task]
retry result  = 'ok' after 3 attempts
```

[ok] priority ordering holds

[ok] retry succeeded on attempt 3

## Act IV — Daemon & CLI

The daemon owns the databases and the control socket; the CLI is a
thin client that never constructs an orchestrator.  This act starts
a real daemon, drives it with real CLI subprocesses, and shuts it
down — flags after the subcommand throughout (the position that
works in both the fixed and unfixed CLI).

### Chapter 19: Daemon + thin client

Step by step, through the real CLI.  First, mutating commands refuse
cleanly when no daemon is running — exit 2, with guidance, no
traceback:

```text
cairn spawn (no daemon)
exit code: 2
No Cairn daemon is running.
  Start one with:  cairn up
  Or run this task inline with:  cairn run <task>
```

[ok] spawn without a daemon exits 2 with guidance

Now the daemon starts.  Ownership is the control socket under
``$CAIRN_HOME/state/``; the project's ``.agentfs/`` holds the
metadata databases.  A second ``cairn up`` against the same home is
refused instead of crashing:

```text
daemon up
control socket present : True ($REPO/demo/out/act4-home/state/orchestrator.sock)
project .agentfs/bin.db: True
```

```text
cairn up (second time)
exit code: 1
A Cairn daemon is already running for this project/cairn-home.
  Stop it first, or use 'cairn queue'/'cairn run' for new tasks.
```

[ok] the control socket is the ownership primitive

[ok] a second cairn up is refused cleanly

``cairn run`` (the inline path) is refused while a daemon owns the
home — the daemon would fight it for the databases:

```text
cairn run while the daemon is live
exit code: 2
A daemon is running; use 'cairn queue' instead.
```

[ok] cairn run while live defers to the daemon

Work goes in over the socket.  ``cairn queue`` submits at NORMAL
priority; the daemon answers synchronously, and the mirror's
``list-agents`` is the public read path for the new id:

```text
cairn queue (over the socket)
exit code: 0
submitted queue
```

[ok] queue accepted the task over the socket

```text
cairn list-agents
agent-8ebf91a0	reviewing	tasks/act4_task.py
... (the mirror holds every agent from this walkthrough)
```

The daemon runs the agent in its own process; we poll the status until it settles:

```text
cairn status agent-8ebf91a0
{
  "agent_id": "agent-8ebf91a0",
  "claim_mismatch": false,
  "error": null,
  "files_deleted": 0,
  "files_written": 2,
  "state": "reviewing",
  "submission": {
    "changed_files": [
      "src/added.py",
      "src/main.py"
    ],
    "submitted_at": 1786294344.3902984,
    "summary": "added a module and updated main"
  },
  "task": "tasks/act4_task.py"
}
```

[ok] the agent reaches REVIEWING through the daemon

[ok] the daemon computed the ground-truth changeset

The review surface, through the CLI:

```text
cairn preview changes agent-8ebf91a0
exit code: 0
added      src/added.py
modified   src/main.py

total changes: 2
```

[ok] preview shows the added and modified files

Accept applies the changeset to the real tree; undo reverses it.
The accept added ``src/added.py`` — the exact case that used to
break undo (the drift check read presence as drift).  Now the added
path's accepted state is presence, so undo deletes it cleanly:

```text
cairn accept agent-8ebf91a0
exit code: 0
accept agent-8ebf91a0: 2 file(s) written, 0 deletion(s) applied
```

[ok] accept applied the changeset

[ok] the added file landed in the tree

```text
cairn undo agent-8ebf91a0
exit code: 0
undo agent-8ebf91a0: 1 file(s) written, 1 deletion(s) applied
```

[ok] undo reversed the accept (added file deleted)

[ok] the tree is back to its pre-accept state

Finally, shutting the daemon down removes the socket — ownership is
gone, and the thin client refuses again:

```text
after shutdown
socket still present : False
exit code (spawn): 2
No Cairn daemon is running.
  Start one with:  cairn up
  Or run this task inline with:  cairn run <task>
```

[ok] shutdown removed the control socket

[ok] spawn exits 2 again after shutdown

### Chapter 20: Recovery after a daemon death

**Skipped loudly.**  This chapter kills a daemon mid-run and
verifies recovery; it is the most likely to flake under load, so
it is off by default.  Re-run with ``--include-recovery`` to
exercise it.

[ok] ch20 skipped by default (--include-recovery required)
